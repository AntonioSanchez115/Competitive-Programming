#include <bits/stdc++.h>
using namespace std;

int main() {
    double l, c;
    cin >> l >> c;
    cout << ((l - 1) * (c - 1)) + l * c << '\n';
    cout << (l - 1) * 2 + (c - 1) * 2 << '\n';
    return 0;
}
